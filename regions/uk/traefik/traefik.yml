entryPoints:
  web: { address: ":80" }
  websecure: { address: ":443" }
certificatesResolvers:
  letsencrypt:
    acme:
      email: "steve@rmy.software"
      storage: /letsencrypt/acme.json
      httpChallenge: { entryPoint: web }
api: { dashboard: true }
providers: { docker: {} }
